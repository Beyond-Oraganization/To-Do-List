package GUI;

import javax.swing.*;
import java.awt.*;

public class SidePanel extends JPanel {
    public SidePanel() {
        super();
        setBackground(new Color(0x606C38));
        setPreferredSize(new Dimension(150, 150));
        setLayout(new BorderLayout());
    }
}
